void main() {
  int? x;
  print(x);
}
  



 
 

 



//Null safety means a variable cannot contain a 'null' value unless you initialized with null to that variable.
//Null safety makes code safer & run faster.
// With null safety, all the runtime null-dereference errors will now be shown in compile time.
// we use two operators in null safety (?,!).
// Null safety prevents errors the result from unintentional access of variables set to null.